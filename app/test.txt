import tkinter as tk
from com.comi import CommissionerManager




def main():
    global root, status_label

    root = tk.Tk()
    root.title("Commissioner Manager")

    # Button to open add commissioner window
    tk.Button(root, text="Add or remove Commissioner ", command=open_add_commissioner_window).pack()

    # Status label
    status_label = tk.Label(root, text="")
    status_label.pack()

    root.mainloop()

if __name__ == "__main__":
    main()